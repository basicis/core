<?php
$files = [
    "test.db",
    "storage/test.db",
    "bin/test.db",
    ".env.test",
    "cache"
];

foreach ($files as $file) {
    if(file_exists($file)) {
        unlink($file);
    }
}
return 0;